# 处理视频流

本项目用于处理视频流文件，实现视频的上传、处理与下载功能。

## 功能简介

- 支持多种视频格式的上传
- 实时处理视频流（如转码、剪辑等）
- 支持处理后的视频文件下载

## 使用方法

1. 克隆本仓库到本地
2. 安装依赖
3. 运行主程序，按照提示上传并处理视频

## 目录结构

- `main.py`：主程序入口
- `utils/`：工具函数
- `static/`：静态文件目录
- `README`：项目说明文档

## 运行
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

## 注意事项

- 确保本地安装了ffmpeg

## 联系方式

如有问题，请联系项目维护者。
## 安装ffmpeg
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install ffmpeg